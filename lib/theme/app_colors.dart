import 'package:flutter/material.dart';

class AppColors {
  // =======================
  // BRAND — LIGHT MODE
  // =======================

  static const primaryDark = Color(0xFF0A2048);
  static const primaryBlue = Color(0xFF166AFF);
  static const primaryLight = Color(0xFFE9EEF9);
  static const primaryWhite = Color(0xFFFFFFFF);

  // =======================
  // SECONDARY — LIGHT MODE
  // =======================

  static const secondaryBlue = Color(0xFFD6E1F5);
  static const secondaryGray = Color(0xFF9CA3AF);
  static const secondaryLight = Color(0xFFF4F6FB);
  static const secondaryMuted = Color(0xFF6B7280);

  // =======================
  // CHART COLORS — LIGHT MODE
  // =======================

  static const chart1 = Color(0xFFF97316);
  static const chart2 = Color(0xFF14B8A6);
  static const chart3 = Color(0xFF1E3A8A);
  static const chart4 = Color(0xFFFACC15);
  static const chart5 = Color(0xFFF59E0B);

  // =======================
  // SUCCESS / WARNING / ERROR — LIGHT MODE
  // =======================

  static const success = Color(0xFF16A34A);
  static const utilitySuccessLite = Color(0xFFCDEEDC);
  static const warning = Color(0xFFF59E0B);
  static const utilityWarningLite = Color(0xFFFBF3C9);
  static const error = Color(0xFFDC2626);
  static const utilityErrorLite = Color(0xFFF4C7C3);

  // LIGHT MODE
  static const lightBackgroundPrimary = Color(0xFFF6F7FA);
  static const lightBackgroundSecondary = Colors.white;
  static const lightSurface = Colors.white;
  static const lightSurfaceElevated = Color(0xFFF9FAFB);

  static const lightBorderSubtle = Color(0xFFE5E7EB);
  static const lightBorderStrong = Color(0xFFD1D5DB);

  static const lightTextPrimary = Color(0xFF111827);
  static const lightTextSecondary = Color(0xFF374151);
  static const lightTextMuted = Color(0xFF6B7280);

  // DARK MODE (NO PURE BLACK)
  //
  // // =======================
  // BRAND — DARK MODE
  // =======================

  static const primaryDarkDark = Color(0xFFE6EAF2);
  static const primaryBlueDark = Color(0xFF5B8CFF);
  static const primaryLightDark = Color(0xFF2A3448);
  static const primaryWhiteDark = Color(0xFF262626);

  // =======================
  // SECONDARY — DARK MODE
  // =======================

  static const secondaryBlueDark = Color(0xFF4F6FB3);
  static const secondaryGrayDark = Color(0xFF5A5F6B);
  static const secondaryLightDark = Color(0xFF2F3442);
  static const secondaryMutedDark = Color(0xFF3F4554);

  // =======================
  // UTILITY — DARK MODE
  // =======================

  static const utilityErrorDark = Color(0xFFE5484D);
  static const utilityErrorLightDark = Color(0xFF7A2E2F);

  static const utilitySuccessDark = Color(0xFF22C55E);
  static const utilitySuccessLightDark = Color(0xFF1F4F39);

  static const darkBackgroundPrimary = Color(0xFF121417);
  static const darkBackgroundSecondary = Color(0xFF1A1D21);
  static const darkSurface = Color(0xFF20242A);
  static const darkSurfaceElevated = Color(0xFF262B33);

  static const darkBorderSubtle = Color(0xFF2A2F36);
  static const darkBorderStrong = Color(0xFF3A3F46);

  static const darkTextPrimary = Color(0xFFE5E7EB);
  static const darkTextSecondary = Color(0xFFB0B7C3);
  static const darkTextMuted = Color(0xFF7A828E);
}
